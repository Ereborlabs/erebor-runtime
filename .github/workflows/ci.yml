name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  helm:
    name: Mithril Helm package
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Verify Mithril Helm package
        run: bash packaging/mithril/helm/tests/verify.sh

  rust:
    name: Rust workspace
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Install native build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler libglib2.0-dev libostree-dev

      - name: Verify Rust workspace
        run: bash .github/scripts/verify-rust-ci.sh
