# Claude Attribution V1

Status: recovery hierarchy restored; original phase-plan body was not
recovered.

Parent plan: [Context DAG](../context-dag.md).

Claude Attribution V1 is a nested Context DAG subplan, alongside Codex
Attribution V1. Its purpose is to ensure Claude-specific prompt, turn, tool,
and physical-effect attribution uses the shared immutable context graph rather
than a parallel context store.

No recovered Claude-specific phase document is sufficient to reconstruct the
original implementation sequence. This master subplan therefore authorizes no
implementation and records no fabricated phase scope. The companion
implementation folder preserves its required place in the hierarchy until the
original phase material is recovered.

Implementation subplan:

- [Claude Attribution V1 implementation](claude-attribution-v1/README.md)
