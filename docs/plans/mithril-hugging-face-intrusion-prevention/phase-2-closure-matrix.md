# Phase 2 Closure Matrix

Status: Blocked.

This table is the exact 29-fixture Phase 2 allocation in
`IdentityTestRunner`. A row is complete only when it has a source-backed Rust
fixture, a physical VM result, a readable shell when an operator can run the
case, and an acceptance record that states the exact limit.

Phase 3 and Phase 4 relationship, permission, raced-policy, and physical-effect
results are not Phase 2 gates. This table records only task, process, execution,
entry, runtime-binding, coordinate, authorization-identity, and native-reference
results.

| Fixture ID | Rust fixture | Manual shell | Physical result or exact missing limit | Status |
| --- | --- | --- | --- | --- |
| `AUTHORIZATION-REPLAY-004` | `run_authorization_replay_fixture` in `IdentityTestRunner` uses the production administrative-envelope encoder and `AuthorizationProofOwner`. | Not operator-runnable; the runner owns the signed bytes, trusted clock, boot identity, and replay WAL. | Pass: retarget, expiry, signature mismatch, same-owner replay, owner-restart replay, and replay after a changed boot identity reject; fresh exact envelopes pass before and after the boot-identity change. The five-record WAL SHA-256 is `2ca8c993cab4371f8d76c35fecc9eedc8044fc3502aa8144c62be6b1b39c399a`. Schema-24 VM JSON SHA-256 `9a0aca62b808421552029518dc4212ed5f1317d483afcc4d6a32b78554228951`. Exact limit: the fixture reloads the production owner with a distinct boot identity; it does not reboot the VM. Phase 4 owns approved exec and protected effects. | Done |
| `ENTRY-BINDING-GAP-001` | `NativeProcessFixture` in `physical_probe`. | `binding-gap.sh` | Pass: pre-binding root is `restored_or_unknown_root` and `fail_closed_unknown`; later root is restricted external. | Done |
| `ENTRY-CONTAINERS-001` | `physical_kubernetes_containers_probe` in `IdentityTestRunner` runs one regular init, native sidecar, and application in one Pod. | `kubernetes-containers.sh` | Pass: the three containers have distinct root task/process identities and distinct execution-set IDs while they share the Pod sandbox and volume. Schema-18 VM JSON SHA-256 `dfb7b407b8a945c474a210fb769abbc09b03599ecb271f4c27cb9d195da92ada`. Exact limit: late discovery gives each root conservative identity; shared-resource policy belongs to later phases. | Done |
| `ENTRY-EPHEMERAL-001` | `physical_kubernetes_ephemeral_probe` in `IdentityTestRunner` creates a targeted ephemeral container. | `kubernetes-ephemeral.sh` | Pass: the target and ephemeral container share one Pod sandbox and PID namespace but keep distinct cgroups, task/process roots, execution sets, and profiles. Schema-19 VM JSON SHA-256 `ee12bc57c8431ac801ae6e06e2e55dbf75ec50692b3a594785fc0d27fabf0efc`. Exact limit: late discovery gives both roots conservative identity; shared-namespace policy belongs to later phases. | Done |
| `ENTRY-EXEC-001` | `physical_kubernetes_probe` covers non-TTY and TTY `kubectl exec`, `kubectl cp`, and an identical native-child control. | `kubernetes-exec.sh`, `kubernetes-exec-tty.sh`, `kubernetes-copy.sh`, and `kubernetes-native-child.sh` | Pass: the three independent exec/copy roots are restricted external roots; the child keeps native parent lineage and the same restricted role. Schema-15 VM JSON SHA-256 `ef749b5a6d2521c6bd865317ce3843bf685610d009500f6d37569c9bd26a57cc`. Approved administrative exec belongs to Phase 4. | Done |
| `ENTRY-EXEC-002` | `physical_kubernetes_probe`. | `cri-exec.sh` | Pass: direct CRI exec is a separate restricted external root. VM JSON SHA-256 `aa70c2c398c6d07d138b81293103f3cbfc4be91d2c8999387b893ff7cac92910`. | Done |
| `ENTRY-EXTERNAL-AMBIGUITY-001` | Two `NativeProcessFixture` roots in `physical_probe`. | `external-ambiguity.sh` | Pass: distinct task/process identities keep the same restricted external class. | Done |
| `ENTRY-LOSS-001` | Missing. | Missing. | Missing independent runtime-binding, entry-evidence, and audit-fact loss. Effect decisions belong to later phases. | Open |
| `ENTRY-MIGRATE-001` | `CloneIntoCgroupFixture` in `physical_probe`. | `nsenter-move.sh` | Pass: namespace entry grants no identity; cgroup entry creates a restricted external root. Phase 4 owns protected effects. Phase 12 owns checkpoint restore through `ENTRY-RESTORE-001`. | Done |
| `ENTRY-NETPROBE-001` | `physical_kubernetes_network_probe` in `IdentityTestRunner` runs real HTTP, TCP, and gRPC readiness probes and samples each live CRI cgroup. | `kubernetes-network-probes.sh` | Pass: each cgroup contained only its container init PID across repeated samples after all three probes made the Pod Ready. Schema-17 VM JSON SHA-256 `cbc024f56ce366a84aa2b0ffdbb7efaab58599b282d1f24295f30c08702fac07`. Exact limit: this proves that these native network probes create no in-container task; network flow and policy belong to later phases. | Done |
| `ENTRY-POSTSTART-001` | `physical_kubernetes_poststart_probe` in `IdentityTestRunner` uses a synchronous OCI prestart gate and runs real Kubernetes `PostStart` hooks before and after the application entrypoint. | `kubernetes-poststart.sh` | Pass: each application was an initial root before its first exec. Both real `PostStart` orders occurred. Each hook was a distinct restricted external root. Schema-22 VM JSON SHA-256 `f7b1c44d26ad5c3b36b401d5f80e87156594dd790daf965fc65c58760e4e0dcb`. Exact limit: this result qualifies the configured `mithril` RuntimeClass on K3s `v1.35.5+k3s1`, containerd `v2.2.3-k3s1`, and the `io.containerd.runc.v2` handler. | Done |
| `ENTRY-POSTSTART-002` | `physical_kubernetes_poststart_probe` keeps the first real hook in flight across a K3s restart and repeats the live Pod's exact hook command through CRI `ExecSync`. | `kubernetes-poststart.sh` | Pass: the application snapshot stayed unchanged; task cookies `269` and `381` and their process IDs differed; both hooks kept the restricted external role. Exact limit: K3s did not resend the hook automatically. Kubernetes permits duplicate hook delivery but does not guarantee deterministic resend after this restart. The fixture therefore supplies the second exact CRI delivery and does not claim an automatic kubelet replay. | Done |
| `ENTRY-PRESTOP-001` | `physical_kubernetes_prestop_probe` in `IdentityTestRunner` holds a real exec hook during Pod deletion. | `kubernetes-prestop.sh` | Pass: the application identity is unchanged during termination; the hook is a fresh restricted external root; profile task references are `2` while both tasks live and `0` after deletion. Schema-21 VM JSON SHA-256 `4d14142beb3671342c7c6d2c8ed8e5c9d85da730f60ef556f7783f7cd231fcee`. Exact limit: containment policy belongs to Phase 4. | Done |
| `ENTRY-PROBE-001` | `physical_kubernetes_probe_impersonation` in `IdentityTestRunner` runs live startup, readiness, and liveness exec probes concurrently. | `kubernetes-probe-impersonation.sh` | Pass: each stock probe is a distinct restricted external root. Schema-20 VM JSON SHA-256 `abead9ce84882d9ecc69853a417ef39ccd629f0df7de97e4ac0e5eebfd9190a6`. Exact limit: stock Kubernetes supplies no purpose, so all three use one restricted external class. | Done |
| `ENTRY-PROBE-002` | `physical_kubernetes_probe_impersonation` runs a native application child with the probe command during the same live probe interval. | `kubernetes-probe-impersonation.sh` | Pass: the child keeps its creator, real parent, execution set, and inherited role; it receives no external-root or probe class. Exact limit: one held native invocation overlaps one held invocation of each stock probe type. | Done |
| `ENTRY-PROBE-IMPERSONATION-003` | `physical_kubernetes_probe_impersonation` runs the native child, three stock probes, ordinary `kubectl exec`, and direct CRI exec with identical command bytes. | `kubernetes-probe-impersonation.sh` | Pass: all seven task/process identities are distinct; the child stays native and every independent entry is restricted external. Schema-20 VM JSON SHA-256 `abead9ce84882d9ecc69853a417ef39ccd629f0df7de97e4ac0e5eebfd9190a6`. Exact limit: approved-role transition belongs to Phase 4. | Done |
| `ENTRY-RESTART-001` | Missing. | `restart.sh` is readable but not self-contained in the manual VM. | Missing runtime, kubelet, and node restart reconciliation with exact coverage gaps. | Open |
| `ENTRY-REUSE-001` | Missing. | Missing. | Missing PID/TID, namespace, cgroup, Pod, and container lifetime reuse without stale identity. | Open |
| `ENTRY-SLEEP-001` | `physical_kubernetes_lifecycle_sleep_probe` in `IdentityTestRunner` reads the live CRI cgroup while the real lifecycle `sleep` action is pending. | `kubernetes-lifecycle-sleep.sh` | Pass: `cgroup.procs` contained only the container init PID while the Pod was not Ready. The lifecycle action created no in-container task. Schema-16 VM JSON SHA-256 `a62e82352a3153c65895d69265e4e0265d78ec6a76679e50a7d1f0bbcc2804fb`. Exact limit: this proves only the native Kubernetes lifecycle `sleep` action; it does not qualify exec probes or network probes. | Done |
| `ENTRY-START-001` | `physical_kubernetes_probe` starts the Pod before Mithril and records its initial root after live CRI discovery. | Every self-contained Kubernetes shell calls `identity_wait_for_initial_binding`; `kubernetes-exec-tty.sh` is the current recorded run. | Pass: the pre-existing PID 1 has no creator, `restored_or_unknown_root`, and `fail_closed_unknown`. Schema-15 VM JSON SHA-256 `ef749b5a6d2521c6bd865317ce3843bf685610d009500f6d37569c9bd26a57cc`. Exact limit: no first-instruction claim; Phase 4 owns effect denial. | Done |
| `ENTRY-STOCK-HOOK-FAILURE-002` | Missing. | Missing. | Missing stock hook timeout, mismatch, and missing-field identity results. | Open |
| `EXEC-COMMIT-STATE-001` | `NativeProcessFixture` in `physical_probe` covers success, pre-PONR recovery, and a deterministic post-PONR fatal ELF load. | `native-child.sh --failed-exec` covers the runnable pre-PONR case. The fatal post-PONR map state is not operator-runnable because Linux terminates the process. | Pass: post-PONR pending state is `PostPonrFatal`, exec outcome is `OutcomeUnknown`, the coordinate is `Exited`, source restriction remains installed, and target execution never becomes active. Schema-23 VM JSON SHA-256 `f659a8983f7002f8558d88862b2f3500b2e138563c083f308f56ac309f1be8cb`. Exact limit: the manual shell proves only pre-PONR recovery; the runner owns the terminal map oracle. | Done |
| `ID-CGROUP-ESCAPE-001` | `CloneIntoCgroupFixture` in `physical_probe`. | `cgroup-escape.sh` | Pass: moved root keeps identity, becomes fail closed, and cannot use host fallback. | Done |
| `ID-CLONE-CGROUP-002` | `CloneIntoCgroupFixture` in `physical_probe`. | Not operator-runnable; the runner owns the stopped clone and pidfd synchronization. | Pass: child identity exists before its first direct effect. | Done |
| `ID-CREATOR-PARENT-007` | `NativeProcessFixture` in `physical_probe` covers creator exit, double fork, subreaper, PID-namespace init, and deterministic namespace-PID reuse. | `native-child.sh --orphan`, `--double-fork`, `--subreaper`, `--namespace-init`, and `native-pid-reuse.sh` | Pass: namespace PID `2` is reused by host PIDs `96008` and `96009`; task cookies `228` and `234`, process states, and execution IDs are fresh; both creator edges remain task cookie `225`. Schema-23 VM JSON SHA-256 `f659a8983f7002f8558d88862b2f3500b2e138563c083f308f56ac309f1be8cb`. Exact limit: permitted ptrace topology and permission are Phase 4 work. | Done |
| `ID-MOVED-PARENT-FORK-004` | `CloneIntoCgroupFixture` in `physical_probe`. | Not operator-runnable; the runner owns the stopped parent and fork result. | Pass: moved labeled parent cannot fork through host fallback. | Done |
| `ID-MOVED-TASK-EXEC-005` | `NativeProcessFixture` in `physical_probe`. | `native-child.sh --moved-exec` | Pass: moved labeled task keeps identity and cannot exec through host fallback. | Done |
| `ID-TASK-COORD-FINALIZE-006` | `NativeProcessFixture` in `physical_probe` covers allocation, runnable state, non-leader exec, missing `PIDFD_THREAD`, leader-first exit, and deterministic namespace-TID reuse. | `native-pid-reuse.sh` covers the runnable PID-reuse control. The task-storage coordinate and leader-first map states are not operator-runnable. | Pass: ordinary `pidfd_open` rejects the non-leader TID, the existing task coordinate identifies it, and namespace TID `3` is reused by fresh host TIDs `96010` and `96011` with task cookies `240` and `242`. Schema-23 VM JSON SHA-256 `f659a8983f7002f8558d88862b2f3500b2e138563c083f308f56ac309f1be8cb`. Exact limit: map saturation belongs to Phase 4 `LSM-DENY-SATURATION-001`. | Done |
| `NATIVE-STATE-REF-LIFETIME-001` | `physical_probe` reads the existing process, entry, profile-reference, and tombstone maps across leader-first and final-thread exit. | Not operator-runnable; the exact retained-state oracle is available only through the existing pinned maps. | Pass: process, entry, and profile references are each `1` after leader exit and `0` after worker exit; the root tombstone releases first, the worker tombstone stays owned until final exit, the process becomes reclaimable, and the entry becomes draining. Schema-23 VM JSON SHA-256 `f659a8983f7002f8558d88862b2f3500b2e138563c083f308f56ac309f1be8cb`. Exact limit: socket and protected-object effect lifetimes belong to later phases. | Done |

Phase 2 closes only when every row is `Done`. The Phase 4 administrative-exec
and raced-policy fixtures do not block this result.
