# Phase 2 Architectural Simplification Proposal

Status: Active decision record. S1, SS1, and SS3 are approved, implemented,
and verified. S5 is retired because SS1 absorbed its useful capacity
requirements while removing the per-session listener architecture. S2-S4 and
S6-S8 remain the original ownership/hardening tier. SS4 is rejected: it was a
product-lifecycle redesign, not a simplification. The former SS2 is already
required by master-plan Phases 3 and 4 and is retained only as an existing-plan
cross-reference. None of S2-S4 or S6-S8 is approved or implemented.

Parent phase:
[Phase 2: SessionSpec, Active Runners, And Daemon-Failure Contract](phase-2-session-spec-active-runners-and-failure-contract.md)

Reviewed implementation commits:

- `3cce98e6bc4ccd029080e76499d343832ae24376`
- `7628d6501c46b1e47437aaca97cb1e3e2ca6330c`
- `0b1303e1bd2835bc400cbaa62facf2dec4910650`

## Purpose

Simplify the implemented Phase 2 architecture before later phases add
packages, adapters, more runners, PTY support, and additional governed
surfaces.

This document is a review proposal and implementation decision record. The
original `S` tier is preserved even where an item is better classified as
hardening or ownership cleanup. The new `SS` tier is reserved for changes that
actually remove product or runtime architecture. Each suggestion describes:

- what the code does now;
- why the current ownership is difficult to extend or reason about;
- a simpler target;
- exact current and proposed code ownership;
- behavior that must remain unchanged;
- compatibility or product-contract changes;
- risks and acceptance evidence; and
- an independent approve, reject, or revise decision.

The user may approve suggestions independently. Approval of one suggestion
does not approve another suggestion or authorize starting a later phase.

## How To Review This Proposal

For each suggestion, select exactly one decision:

- `[ ] Approve`
- `[ ] Reject`
- `[ ] Revise`

When selecting `Revise`, place the replacement decision under `Review notes`.
Implementation must not begin while any required decision for that suggestion
is unresolved.

Suggestions marked behavior-preserving reorganize ownership without changing
the daemon protocol or session result. Suggestions marked contract-changing
remove or alter currently exposed Phase 2 fields or messages and require an
explicit decision.

For this document, an `SS` suggestion qualifies as architectural
simplification only if it removes at least one long-lived server/process,
public lifecycle operation, durable model, routing layer, or runner extension
point. Moving code between files, adding rollback owners, or adding limits does
not qualify by itself.

## Non-Negotiables

These are not simplification candidates:

- `erebord` continues to host daemon control and runtime guard as two separate
  services with separate listeners, protocols, authentication, dispatch, and
  resource ownership.
- Governed workloads never receive the daemon control socket.
- Each workload receives only a session-local projected runtime-guard endpoint
  and its own session credential. The daemon-side guard listener may be shared
  across sessions; this does not merge runtime guard with daemon control.
- The daemon remains the privileged admission and lifecycle authority.
- `SessionSpec` remains immutable and pins the exact admitted runner
  capability, identities, policy inputs, failure behavior, output
  requirements, and workload privilege constraints.
- The durable session repository, lifecycle generation checks, idempotency,
  safe descriptor-based path resolution, required-output fail-closed behavior,
  root-only audited retention holds, and honest recovery states remain.
- A runner owns its execution and recovery mechanisms. Adding a runner must
  not require embedding that runner's process, container, or orchestration
  behavior in a generic core module.
- Linux is the first implementation, not a universal mechanism imposed on
  macOS, Docker, Kubernetes, or another future runner.
- No compatibility layer is required for the Phase 2 internal daemon-session
  protocol. A breaking cleanup is allowed only when explicitly approved.
- No suggestion may silently weaken containment, interception, output,
  evidence, or daemon-failure guarantees to reduce code.
- Do not implement an unapproved suggestion. S1, SS1, and SS3 are the only
  approved and implemented items in this record. SS4 is explicitly rejected.

## Reviewed Baseline Before S1

This section records the implementation that S1 reviewed. It is intentionally
historical; the S1 implementation result below describes the current owner
boundary.

The reviewed commits add the following main path:

```text
erebor client
  |
  v
daemon control listener
  |
  v
DaemonControlState
  |
  v
DaemonSessionService
  |-- admission and start revalidation
  |-- staging mounts and guard credentials
  |-- output stores and input leases
  |-- terminal resource cleanup
  |
  v
SessionSupervisor
  |-- lifecycle transitions
  |-- active handle map
  |-- start, wait, stop, kill, remove, recovery
  |
  +--> SessionRepository
  |
  +--> RunnerRegistry
         |
         +--> LinuxHostSessionRunner
         +--> DockerSessionRunner
                |
                v
        generic erebor-session-helper
                |
                +--> LinuxWorkload
                +--> DockerWorkload

agent process guard
  |
  v
per-session runtime guard socket
  |
  v
per-session acceptor and worker pool
```

The reviewed behavior was intentionally durable and defensive:

1. The daemon authenticates the Unix peer and decodes a typed request.
2. Mutating requests are made durable through the idempotency store.
3. Create admission resolves caller paths through the descriptor broker,
   snapshots runner capability and policy inputs, and persists `created`
   before starting resources.
4. Start revalidates the admission, transitions to `starting`, creates output
   stores, staging mounts, guard credentials, and a per-session guard service.
5. The selected runner starts a common helper, and the helper starts either a
   Linux process-guard workload or a Docker container.
6. The supervisor persists the stable runner identity and transitions to
   `running`.
7. The helper owns output capture and applies the selected behavior when the
   daemon control pipe reaches EOF.
8. After daemon restart, the supervisor reconstructs a Linux or Docker active
   handle from the durable runner binding rather than reconnecting to the old
   daemon pipe.
9. Terminal reduction stops the guard, removes leases and credentials,
   unmounts staging, and preserves or removes durable records according to the
   requested operation and retention hold.

## Main Simplification Finding

The security contracts are not the primary source of accidental complexity.
The largest issue is that one session lifecycle is coordinated by several
partial owners:

- `DaemonSessionService` prepares and cleans runtime resources;
- `SessionSupervisor` owns state reduction and active handles;
- `SessionRepository` owns durable state validation;
- the core crate owns Linux and Docker launch/recovery implementations; and
- the common helper again selects Linux or Docker workload behavior.

This divides one start or recovery transaction across multiple modules and
repeats runner selection at several layers. Failure cleanup must consequently
be manually coordinated across those owners.

## Decision Index

| ID | Suggestion | Change class | Recommendation | Decision |
| --- | --- | --- | --- | --- |
| S1 | One complete session lifecycle owner | Behavior-preserving | Approve | Implemented and verified |
| S2 | Explicit prepared-session resource owner | Behavior-preserving | Approve | Pending |
| S3 | Runner implementations leave `erebor-runtime-core` | Behavior-preserving boundary change | Approve | Pending |
| S4 | Runner-owned continuity and recovery handle | Architectural and recovery-contract change | Approve with explicit mechanism review | Pending |
| S5 | One daemon-wide runtime-guard capacity owner while retaining per-session listeners | Behavior-preserving resource redesign | Retire in favor of SS1 | Absorbed by implemented SS1 |
| S6 | Compose `SessionSpec` from smaller validated sections | Schema-breaking internal model cleanup | Approve | Pending |
| S7 | Remove or defer fields and APIs with no reachable behavior | Product-contract change | Review item by item | Pending |
| S8 | One typed daemon-command dispatch model | Behavior-preserving, optional | Defer until S1-S7 settle | Pending |

## S1: One Complete Session Lifecycle Owner

### Recommendation

Approve.

### Reviewed Pre-S1 Behavior

`DaemonSessionService` at
`crates/erebor-runtime-daemon/src/session_control.rs:55` owns:

- conversion from admitted requests into session operations;
- start-time root-configuration revalidation;
- safe-path identity revalidation;
- output-store creation;
- staging mounts;
- runtime-guard credential creation and registration;
- input-lease managers;
- runtime cleanup; and
- protobuf response construction.

`SessionSupervisor` at
`crates/erebor-runtime-session/src/session_supervisor.rs:67` separately owns:

- runner discovery and capability comparison;
- lifecycle transitions;
- runner start and recovery;
- the in-memory active-handle map;
- wait, stop, kill, remove, and reconcile reduction; and
- terminal state selection from runner exit facts.

`SessionRepository` at
`crates/erebor-runtime-session/src/session_repository.rs:80` owns durable
generation comparison and validates the lifecycle edge again before writing.

A start currently crosses both `DaemonSessionService::start` at
`session_control.rs:402` and `SessionSupervisor::begin_start/launch_start` at
`session_supervisor.rs:136` and `session_supervisor.rs:164`. Each layer has
its own failure cleanup.

### Problem

There is no single owner of the start, recovery, or terminal-cleanup
transaction.

Examples:

- A staging failure must be converted into a lifecycle failure by one owner
  and cleaned by another.
- A runner-start failure must separately stop the guard and clean mounts.
- `wait`, `stop`, `kill`, and `remove` repeat terminal cleanup outside the
  supervisor that selected the terminal state.
- Repository-only operations such as inspect, list, prune, and retention hold
  pass through supervisor methods that add no domain behavior.

This makes crash points and compensation hard to enumerate.

### Proposed Target

Replace `SessionSupervisor` with one complete `SessionManager` in
`erebor-runtime-session`.

`SessionManager` owns:

- `SessionRepository`;
- `RunnerRegistry`;
- the active-session table;
- prepared runtime resources;
- runtime-guard registrations;
- output stores;
- start, recovery, terminal cleanup, remove, and prune;
- lifecycle transition decisions; and
- root-only retention state mutation as a domain operation.

The daemon-side owner becomes a thin `DaemonSessionApi` that:

- converts protobuf requests into domain commands;
- provides the observed caller identity and current root constraints;
- calls `SessionManager`; and
- converts domain results into protobuf responses.

Safe caller-path resolution remains a real privilege boundary. The manager
receives a narrow safe-path resolver collaborator; it does not open raw
caller paths itself.

Proposed ownership:

```text
crates/erebor-runtime-session/src/session_manager.rs
crates/erebor-runtime-session/src/session_manager/
  active.rs
  lifecycle.rs
  recovery.rs
  resources.rs

crates/erebor-runtime-daemon/src/session_api.rs
crates/erebor-runtime-daemon/src/session_api/
  admission.rs
  protocol.rs
  response.rs
```

The exact file split is secondary. The required architecture is one lifecycle
owner and one daemon protocol adapter.

### Behavior That Must Remain

- Create persists `created` before creating live resources.
- Start is legal exactly once unless an idempotency replay resumes the same
  pending operation.
- Runner capability and root constraints are revalidated immediately before
  start.
- No workload reaches `running` before the stable runner binding is durable.
- Every failure point produces an honest durable state.
- Terminal cleanup is idempotent and safe to retry after daemon restart.
- Retention hold continues to block removal and prune.

### Risks

- Moving root-resource preparation into the lifecycle transaction could
  accidentally make the session crate responsible for request authentication.
  It must not; the daemon remains responsible for peer authentication and
  authorization.
- A manager that directly depends on daemon configuration would recreate the
  same boundary under another name. Pass a validated root-constraint snapshot
  instead.
- One large manager file would not be a simplification. The owner may use
  focused collaborators, but only the manager commits lifecycle outcomes.

### Acceptance Evidence

- Fault injection at every start step proves one cleanup path and one final
  durable state.
- Replaying the same idempotent start across each crash point produces no
  duplicate helper, guard, mount, process, or container.
- Stop, kill, wait, natural exit, failed required output, remove, and recovery
  all invoke the same terminal finalization owner.
- Existing repository generation-race tests remain.

### Decision

- [X] Approve
- [ ] Reject
- [ ] Revise

Review notes:

### Implementation Result

State: **Done**.

S1 now has one lifecycle transaction owner:

- `crates/erebor-runtime-session/src/session_manager.rs` owns the repository,
  runner registry, active handles, input leases, lifecycle transitions, start,
  wait, stop, kill, remove, prune, recovery, and terminal finalization;
- `crates/erebor-runtime-session/src/session_manager/resources.rs` is the
  manager-owned runtime collaborator for safe-path revalidation, staging,
  output stores, guard credentials and registrations, stream reads, and
  idempotent runtime cleanup;
- `crates/erebor-runtime-daemon/src/session_api.rs` is the protocol and
  admission adapter. It validates current root configuration, calls the
  manager, and renders protobuf responses; and
- `DescriptorBroker` implements the narrow `SessionPathResolver` seam, while
  the daemon supplies the admitted interception-router factory. This keeps
privileged caller-path resolution and Phase 2 policy construction outside
the generic manager.

An admitted start must carry `ValidatedStartConstraints`, which binds the
observed owner, session ID, and current root-configuration generation. The
daemon constructs it only after checking the active root limits and pinned
fixture; the manager rejects a missing, stale, or mismatched validation before
creating resources.

The lifecycle manager now performs path identity revalidation before creating
start resources, records preparation and runner-start failures durably, owns
recovery preparation and reduction, retries terminal cleanup during replay and
prune, and removes leases through the same cleanup path used by wait, stop,
kill, remove, and recovery.

At the original S1 checkpoint, the daemon control listener and per-session
runtime guard listeners remained separate services. S1 changed their lifecycle
ownership, not their protocol, listener, or authentication boundaries. The
later approved SS1 implementation below replaces the guard-side per-session
listeners without merging guard traffic into daemon control.

S2 was not implemented: `OutputEndpoints` remains the runner input,
`SessionLayout` and `PreparedSession` do not exist, and cleanup remains an
explicit idempotent manager operation rather than an RAII resource transfer.
S3-S8 also remain pending.

Code-backed evidence added to the manager covers once-only start and replay,
capability drift, preparation failure, runner-start failure, wait and stop
finalization, stable-binding recovery, recovery preparation failure,
terminate-on-daemon-loss, missing-binding interruption, persisted stop intent,
kill, remove, and cleanup ownership.

Exact repository and privileged verification is recorded at the end of this
document.

## S2: Explicit Prepared-Session Resource Owner

### Recommendation

Approve. This suggestion supports S1 but can be reviewed independently.

### Original Behavior (Removed By SS3)

`OutputEndpoints` at
`crates/erebor-runtime-core/src/session.rs:43` begins as five output paths but
also accumulates:

- runtime environment variables;
- a prepared workspace path; and
- a prepared executable path.

`DaemonSessionService::prepare_runtime` at
`crates/erebor-runtime-daemon/src/session_control.rs:638` creates output
stores, staging mounts, guard credentials, a guard registration, and the
runner environment, then returns that `OutputEndpoints` value.

Cleanup is manual in:

- `DaemonSessionService::start`;
- `DaemonSessionService::finalize_runtime_resources`; and
- `DaemonSessionService::cleanup_staging`.

### Problem

`OutputEndpoints` is not an output-only value. It is an incomplete runtime
resource bundle without ownership or rollback semantics.

If preparation fails after mounting a workspace or starting a guard, callers
must know exactly which earlier steps succeeded. New resources added in later
phases would add more compensation branches.

Absolute output and runtime paths are also constructed in several locations
from UID, session ID, and configured roots.

### Proposed Target

Introduce two owners:

`SessionLayout`

- deterministically derives state, output, runtime, staging, credential, and
  socket paths from trusted roots, owner UID, and session ID;
- contains no IO and no user-controlled path resolution.

`PreparedSession`

- owns successfully created output stores, staging mounts, resolved execution
  paths, guard registration, runtime environment, and runner-specific prepared
  input;
- automatically rolls back owned resources if start fails before activation;
- transfers cleanup ownership to the active session entry only after the
  runner binding and `running` state are durable; and
- supports idempotent reconstruction during daemon recovery.

Proposed locations:

```text
crates/erebor-runtime-session/src/session_layout.rs
crates/erebor-runtime-session/src/session_manager/resources.rs
```

`OutputEndpoints` is removed. A runner receives an immutable
`PreparedSession` view containing only the resources admitted for that runner.

### Behavior That Must Remain

- Required stdout/stderr configuration continues to come from `OutputPlan`.
- Output stores exist before the runner starts.
- A user path is never reopened by pathname as root.
- Docker receives only daemon-staged paths.
- Linux receives only its private projected resources.
- Guard credentials are never included in public session responses or normal
  workload output.

### Risks

- Destructor-only cleanup cannot report errors. Explicit `rollback()` and
  `finalize()` operations must return cleanup failures; `Drop` is only the
  last-resort best effort.
- Recovery must verify existing staging resources rather than recreate or
  trust them blindly.
- A generic resource bag with many optional fields would repeat the existing
  problem. Runner-specific prepared data should remain runner-owned.

### Acceptance Evidence

- Tests fail preparation after each acquired resource and prove no leaked
  mount, guard socket, credential, output handle, process, or container.
- Repeated rollback and terminal cleanup are harmless.
- `SessionLayout` path tests cover UID/session separation and unsafe session
  identifiers.

### Decision

- [x] Approve
- [ ] Reject
- [ ] Revise

Review notes:

## S3: Runner Implementations Leave `erebor-runtime-core`

### Recommendation

Approve.

### Current Behavior

The generic traits are correctly defined in
`crates/erebor-runtime-core/src/session.rs:191` and
`crates/erebor-runtime-core/src/session.rs:205`.

However, the same core crate also owns:

- `SessionHelperLaunchConfig` in
  `crates/erebor-runtime-core/src/session/active.rs:26`;
- `LinuxHostSessionRunner` in
  `crates/erebor-runtime-core/src/session/linux_host.rs:22`;
- `DockerSessionRunner` in
  `crates/erebor-runtime-core/src/session/docker.rs:22`;
- helper process launching and the JSON helper protocol;
- Linux recovery in `active.rs:368`;
- Docker recovery in `active.rs:488`;
- Docker CLI inspection;
- process start-time parsing and process-group signaling;
- `LinuxHostSessionRunner`; and
- `DockerSessionRunner`.

Both runner structs also serve the old foreground CLI path and the new
daemon-owned active-session path.

### Problem

Adding a runner requires editing generic core matches and helper matches.
Linux/Docker process behavior causes `erebor-runtime-core` to depend on
`rustix`, and core knows about helper paths, Docker paths, systemd paths,
container inspection, and Linux process identity.

The `SessionRunner` seam therefore exists, but its implementations are still
centralized on the generic side of the seam.

### Proposed Target

Keep only generic session contracts in `erebor-runtime-core`:

```text
crates/erebor-runtime-core/src/session/
  spec.rs
  lifecycle.rs
  runner.rs
```

Move daemon-owned implementations to:

```text
crates/erebor-runtime-session/src/runners.rs
crates/erebor-runtime-session/src/runners/
  linux.rs
  linux/
    ...
  docker.rs
  docker/
    ...
```

Each runner owns:

- capability inspection;
- runner-specific admission validation;
- preparation requirements;
- start;
- observation;
- signal semantics;
- recovery proof;
- runner-resource cleanup; and
- encoding and validation of its durable recovery handle.

`RunnerRegistry` is the only component that selects a runner.

### Runner Identifier Decision

The current `SessionRunnerKind` enum requires central edits for every new
runner.

Recommended target: a validated `RunnerId` such as `linux-host`, `docker`,
`macos`, or `kubernetes`, used as the registry key and persisted in
`SessionSpec`.

Alternative: retain `SessionRunnerKind` while moving implementations. This is
less disruptive but preserves the central extension point.

Select one:

- [ ] Use validated extensible `RunnerId` throughout the daemon-owned path
- [ ] Retain the enum and only move implementations
- [ ] Revise

### Behavior That Must Remain

- The same selected runner performs inspect, validate, start, recover, signal,
  and cleanup.
- The exact capability snapshot remains durable.
- Start and recovery fail if the current implementation cannot satisfy the
  admitted snapshot.
- Linux and Docker retain their current containment and no-implicit-pull
  guarantees.

### Risks

- Maintaining separate foreground and daemon runner implementations for too
  long would create duplication. The foreground implementation is removed
  when its approved CLI migration phase lands.
- An opaque string runner ID still needs strict validation and collision
  rules.
- Runner registration must be explicit; runtime loading of arbitrary code
  into the privileged daemon is not introduced.

### Acceptance Evidence

- Core has no Docker CLI, systemd, helper-process, Linux process, or recovery
  implementation.
- A fake runner can be registered in tests without changing a central enum
  match.
- Linux and Docker capability/start/recovery tests live beside their runner
  owners.

### Decision

- [ ] Approve
- [ ] Reject
- [ ] Revise

Review notes:

## S4: Runner-Owned Continuity And Recovery Handle

### Recommendation

Approve the ownership rule. Review the exact Linux and Docker mechanisms
before implementation.

### Current Behavior

Before SS3, `SessionHelperLaunchConfig` in core started one common
`erebor-session-helper`. SS3 removed this shared protocol and universal
dispatch; S4 remains relevant only as a review of each driver's now-opaque
recovery schema and continuity proof.

The helper receives a `SessionHelperHandoff` containing:

- the complete `SessionSpec`;
- output and continuity paths;
- prepared workspace/executable paths;
- runtime environment;
- process-guard and Docker executable paths; and
- systemd unit and slice names.

`HelperWorkload` at
`crates/erebor-runtime-session/src/session_helper/workload.rs:25` selects
`LinuxWorkload` or `DockerWorkload`.

The initial daemon controls the helper over inherited stdin/stdout. After
daemon loss, that pipe reaches EOF and cannot be reconnected. A restarted
daemon does not regain the helper protocol. Instead:

- `RecoveredLinuxSession` parses the saved process-group and helper PID from a
  semicolon-delimited string and signals the process group directly.
- `RecoveredDockerSession` parses the container/helper identities, inspects
  Docker directly, and controls the container directly.
- Both use helper liveness as part of recovery proof, while durable continuity
  evidence is not the sole recovery authority.

The complete Linux `continue` acceptance currently fails because the saved
process identity is no longer live after daemon SIGKILL.

### Problem

The common helper is a Linux implementation mechanism presented as a generic
runner abstraction.

Runner choice appears in:

- the registry;
- core runner implementations;
- helper workload selection; and
- core recovery implementations.

The saved `RunnerBinding::stable_identity` is a string with runner-specific
format and delimiters. Generic code parses runner-specific recovery facts.

### Proposed Target

Make continuity part of the runner contract:

```text
Runner
  inspect()
  validate_admission()
  prepare()
  start() -> ActiveSession + RecoveryHandle
  recover(RecoveryHandle) -> RecoveryObservation
  signal()
  cleanup()
```

`RecoveryHandle` is:

- versioned;
- opaque outside the selected runner;
- durably stored with runner ID and implementation ID;
- bounded in size;
- validated by the runner before use; and
- never interpreted by generic core or daemon protocol code.

Each runner chooses mechanisms suitable to its execution environment. A
runner may use an operating-system service manager, cgroup identity, a
runner-specific continuity process, a container runtime, or an orchestrator.
No one mechanism becomes a requirement for every runner.

Shared internal libraries are allowed where they remove duplication, but a
shared library does not own runner selection.

### Mechanism Decisions Required

Linux-host:

- [ ] Keep a Linux-specific continuity helper and define durable recovery
      evidence that a restarted daemon can verify
- [ ] Use the Linux service/cgroup manager as the primary durable workload
      identity and retain only the minimum helper needed for output or loss
      behavior
- [ ] Revise

Docker:

- [ ] Use Docker container identity as the primary recovery handle and retain
      a runner-owned loss watcher only where required
- [ ] Keep a Docker-specific continuity helper with explicit durable recovery
      evidence
- [ ] Revise

The selected mechanisms must separately prove `terminate` and `continue`.

### Behavior That Must Remain

- `terminate` kills the complete admitted workload after daemon loss.
- `continue` preserves the workload, output, and sufficient recovery evidence.
- A restarted daemon returns to `running` only after proving runner identity,
  containment, guard/evidence continuity, and output continuity.
- A missing or mismatched recovery fact results in an honest terminal or
  interrupted state.
- Recovery never trusts a PID by itself.

### Risks

- Removing the common helper without replacing its daemon-loss observation
  would break `terminate`.
- Treating a container ID, PID, or unit name as sufficient identity would
  weaken recovery.
- A reconnectable helper endpoint would become another authenticated service
  and requires an explicit security design; it must not be introduced
  implicitly.
- Runner-specific mechanisms can share contract tests, but their internals
  should not be forced into identical shapes.

### Acceptance Evidence

- Direct daemon SIGKILL and graceful service restart pass for every advertised
  runner/failure-mode pair.
- Recovery-handle corruption, reuse, stale identity, missing helper/watcher,
  and changed containment all fail honestly.
- The Linux `continue` scenario returns to `running` only when continuity is
  proven.
- The complete helper/process/container tree is absent after `terminate`.

### Decision

- [ ] Approve the runner-owned continuity rule
- [ ] Reject
- [ ] Revise

Review notes:

## S5: Retired — Capacity-Only Sharing With Per-Session Listeners

### Recommendation

Retire this as an independent suggestion. SS1 selected and implemented the
simpler architecture: one daemon-wide runtime-guard listener, server, queue,
worker pool, and capacity owner with logical per-session registrations.

### Historical Behavior Before SS1

`RuntimeGuardService` at
`crates/erebor-runtime-session/src/runtime_interception_broker/service.rs:23`
stored default limits and created one
`RuntimeInterceptionBrokerServer` per session.

Each Unix server at
`crates/erebor-runtime-session/src/runtime_interception_broker/platform.rs:175`
created:

- one acceptor thread;
- a fixed worker pool, currently four workers by default;
- one bounded queue;
- one connection budget; and
- one shutdown/drain lifecycle.

The hardening commit removed unbounded thread-per-connection behavior, but the
bounds applied to each session independently. There was no daemon-wide
active-session or guard-thread budget.

At those defaults, 100 active guard sessions could create approximately
500 guard threads before counting helper and output-pump threads.

### Historical Problem

`RuntimeGuardService` is the apparent capacity owner, but it multiplies the
same capacity for every session. Total daemon resource use remains
proportional to an otherwise unbounded active-session count.

### Superseded Target

S5 proposed keeping one isolated socket per session while sharing:

- one shared bounded guard executor;
- one global connection budget;
- one global queue;
- smaller per-session connection and queued-request limits for fairness;
- one deadline/cancellation policy;
- service-wide shutdown and drain; and
- per-session unregister/drain without stopping unrelated sessions.

Its proposed ownership was:

```text
runtime_interception_broker/
  service.rs       # session registration and service lifecycle
  executor.rs      # shared queue, workers, global/per-session permits
  platform.rs      # Unix listener/socket operations only
  server.rs        # authenticated protocol dispatch only
```

That design no longer applies. SS1 removes the per-session host listeners and
therefore does not need a second executor abstraction or a platform event
multiplexer across many sockets.

### Requirements Absorbed By SS1

- Daemon control and runtime guard remain separate services.
- Session A cannot authenticate as or dispatch to session B.
- Overload fails closed before domain dispatch.
- Read and write deadlines remain.
- Stopping one session revokes only that logical session registration.
- Stopping the service drains all work within a bounded deadline.
- Global, per-UID, and per-session connection limits share one service owner.

### Risks

- A global queue without per-session limits lets one session starve others.
- Holding global locks during policy evaluation could serialize independent
  sessions.
- Reusing the daemon control executor would merge service resource ownership;
  the runtime guard keeps its own pool.

### Acceptance Evidence

- Starting 1, 10, or 100 sessions does not create listeners or workers per
  session.
- Global connection/queue limits remain fixed under multi-session load.
- Per-session overload does not block a second session.
- Session unregister immediately rejects new traffic for only that session.
- Service shutdown has a deterministic deadline and leaves no sockets or
  workers.

### Decision

- [X] Retire as an independent suggestion; SS1 owns this behavior

Review notes: S5's capacity requirements remain valid and are implemented by
SS1. Its defining architectural constraint, retaining per-session host
listeners, is intentionally gone. There is no remaining S5 implementation
slice.

## S6: Compose `SessionSpec` From Smaller Validated Sections

### Recommendation

Approve.

### Current Behavior

`RunRequest` at
`crates/erebor-runtime-core/src/session/admission.rs:714` is a large flat input
value.

`SessionAdmission` at `admission.rs:883` then mirrors almost every field that
will appear in `SessionSpec`.

`SessionSpec` at `admission.rs:912` copies those fields again and owns one long
validation method that mixes:

- generic identity validation;
- runner selection;
- Linux versus Docker execution requirements;
- policy identity completeness;
- capability compatibility;
- path validation;
- output validation;
- TTY/attach assumptions; and
- lifecycle timestamps and configuration generation.

`OutputPlan` also stores an absolute daemon state path even though that path is
deterministically derived from trusted daemon roots, UID, and session ID.

### Problem

The immutable contract is valuable, but its flat representation makes all
changes look like changes to one object. Runner-specific validation lives in
generic spec validation, and construction requires a field-for-field mirror
type.

This will become harder when macOS, Kubernetes, packages, adapters, secrets,
and new governed surfaces are added.

### Proposed Target

Compose the spec from validated domain sections:

```text
SessionSpec
  identity: SessionIdentity
  execution: ExecutionPlan
  governance: GovernancePlan
  continuity: ContinuityPlan
  output: OutputPlan
  admission: AdmissionStamp
```

Suggested responsibilities:

`SessionIdentity`

- session ID;
- owner UID/GID;
- creation time.

`ExecutionPlan`

- runner ID;
- command;
- immutable workspace/executable/image identities;
- environment;
- workload privilege constraints;
- runner-owned admitted execution data.

`GovernancePlan`

- package/installation/adapter identities;
- policy-input identities and effective policy-set digest;
- admitted governed surfaces and evidence requirements.

`ContinuityPlan`

- daemon-failure mode;
- root-capped loss grace;
- required recovery/output continuity.

`OutputPlan`

- maximum bytes;
- rotation;
- page size;
- required/optional streams and evidence.

`AdmissionStamp`

- spec schema version;
- root configuration generation;
- complete runner capability snapshot.

`SessionAdmission` is removed. The admission owner constructs these validated
sections and then constructs `SessionSpec`.

Physical state/runtime/output paths move to `SessionLayout`; immutable limits
and requirements remain in `SessionSpec`.

Generic `SessionSpec` validation checks composition and cross-section
invariants. Runner-specific executable/image/containment validation belongs to
the selected runner.

### Behavior That Must Remain

- The complete effective admission remains serializable and durable.
- Deserialization revalidates every nested section.
- Policy/package/runner identities remain cryptographically pinned.
- The exact runner capability snapshot remains part of the immutable record.
- Runtime paths remain daemon-derived and cannot become user-selected.

### Risks

- Splitting one flat object into many empty wrappers would add ceremony rather
  than simplify ownership. Each section must own real validation and behavior.
- Runner-specific admitted data must remain versioned and bounded.
- Moving absolute paths out of the spec must not make recovery depend on
  ambient current configuration; `SessionLayout` uses the durable session
  owner/ID and the installed daemon roots.

### Acceptance Evidence

- No field-for-field `SessionAdmission` mirror remains.
- Generic spec validation contains no Linux/Docker match.
- Round-trip and malformed nested-section tests cover every section.
- Existing records are intentionally migrated or rejected according to the
  approved no-compatibility decision; no silent reinterpretation occurs.

### Decision

- [ ] Approve
- [ ] Reject
- [ ] Revise

Review notes:

## S7: Remove Or Defer Contracts With No Reachable Behavior

### Recommendation

Review each item independently. Do not retain a field merely because a later
phase might use the same name.

### Current Findings And Proposed Decisions

The main current locations are:

- `SessionCreateRequest`, `SessionWaitRequest`, attach, and input-lease
  messages in
  `crates/erebor-runtime-ipc/proto/erebor/runtime/ipc/v1/daemon.proto:45`;
- the corresponding client methods in
  `crates/erebor-runtime-client/src/session.rs`;
- daemon dispatch in `crates/erebor-runtime-daemon/src/control.rs`;
- domain handling in
  `crates/erebor-runtime-daemon/src/session_control.rs`;
- stored spec fields in
  `crates/erebor-runtime-core/src/session/admission.rs:714`; and
- `InputLeaseManager` in
  `crates/erebor-runtime-session/src/session_output/input_lease.rs:39`.

| Contract | Current behavior | Recommended Phase 2 action |
| --- | --- | --- |
| `SessionCreateRequest.detached` and `SessionSpec.detached` | Stored, exposed by getters, and never used to select lifecycle behavior. Every daemon-owned session already survives client disconnect. | Remove. Detached daemon ownership is the only Phase 2 behavior. |
| `SessionWaitRequest.after_generation` | Sent by the client but ignored by daemon wait. Wait blocks toward terminal completion rather than “next change after generation.” | Remove now, or explicitly implement generation-watch semantics. Do not keep an ignored cursor. |
| `SessionAttachRequest.after_output_sequence` | Sent but ignored. | Remove with the current attach request. Logs/events already own durable cursors. |
| Read-only attach | Returns metadata but establishes no output or terminal transport. | Remove until real attach transport exists. Continue using logs/events. |
| Input lease protocol and `InputLeaseManager` | Implemented, but both initial runners reject TTY, so no session can receive a lease. | Remove from Phase 2 and reintroduce with real PTY transport in Phase 6. |
| `secret_references` in create/spec | Validated and persisted but never resolved or injected. | Remove from the public Phase 2 request. Reintroduce through the approved secret-provider/store owner. |
| `ActiveSession::capability_snapshot` | Implemented by active handles but never called by production code. | Remove. The durable spec and selected runner already own the snapshot comparison. |
| Capability `supported_signals` | Advertised but not used to authorize stop/kill requests. | Either enforce it in `SessionManager` or remove it until signal capability differs by runner. Recommendation: enforce. |
| Capability `filesystem_isolation` | Advertised and exposed but not consumed in start admission. | Either make it a required governance capability or remove the generic boolean. Recommendation: replace it with an enforced structured guarantee. |
| `filesystem_projections` | Persisted, but runners primarily use separately prepared paths and hard-coded workload paths. | Make projections authoritative input to `PreparedSession`, or remove until they are. Recommendation: make them authoritative. |
| `endpoint_projections` | Admission records a guard path, while runtime preparation independently creates the endpoint and environment. | Make the projection plan authoritative and materialize it through `PreparedSession`. |
| `evidence_requirements` | Validated and stored, while helper output/evidence behavior is mostly fixed. | Merge required evidence semantics into the enforced output/governance plan. Reject unsupported evidence requirements at admission. |
| `ContinueIfEnforced` | Parsed only to be rejected because both runners lack it. | Keep only if explicit rejection is considered part of the current product contract; otherwise reintroduce when a runner can advertise it. |

### Item Decisions

Detached flag:

- [ ] Remove
- [ ] Keep and implement distinct behavior now
- [ ] Revise

Wait generation cursor:

- [ ] Remove
- [ ] Implement generation-watch semantics
- [ ] Revise

Attach and input lease:

- [ ] Remove until Phase 6
- [ ] Keep the current unreachable foundation
- [ ] Revise

Secret references:

- [ ] Remove until a real secret-provider owner exists
- [ ] Keep but reject non-empty references until implemented
- [ ] Revise

Projection and evidence declarations:

- [ ] Make them authoritative through `PreparedSession`
- [ ] Remove until their implementation phase
- [ ] Revise

`ContinueIfEnforced`:

- [ ] Keep explicit current rejection
- [ ] Remove until supported
- [ ] Revise

### Behavior That Must Remain

- Client disconnect never terminates a daemon-owned session.
- Logs and events retain durable cursor behavior.
- No unresolved secret reference is silently presented as an injected secret.
- No projection or evidence requirement is claimed unless execution enforces
  it.
- Existing Linux/Docker signal operations remain available if their
  capabilities advertise them.

### Acceptance Evidence

- Protocol contract tests prove removed messages/fields are absent.
- Every remaining `SessionSpec` field has at least one production consumer or
  is an immutable identity required for later verification.
- Unknown or unsupported declarations fail admission rather than being stored
  inertly.

### Decision

- [ ] Approve selected item decisions
- [ ] Reject this cleanup
- [ ] Revise

Review notes:

## S8: One Typed Daemon-Command Dispatch Model

### Recommendation

Defer until S1-S7 are decided, then approve if the remaining command set still
has the same duplication.

### Current Behavior

`DaemonControlState::dispatch` at
`crates/erebor-runtime-daemon/src/control.rs:362` matches message-kind strings
to many request-specific methods.

Mutating session methods then:

1. decode a protobuf message;
2. derive UID/session scope;
3. choose an operation string;
4. build a `MutationIntent`;
5. invoke generic idempotency;
6. match the intent again in `DaemonSessionService::apply`; and
7. encode a response kind/payload pair.

Root administrative commands repeat another authorization and audit path.
The client in `crates/erebor-runtime-client/src/session.rs` mirrors the command
and response-kind mapping.

### Problem

Message kind, mutability, authorization scope, operation name, intent, and
response kind are related facts represented in different match statements and
string constants.

Adding a command can compile while one metadata path is omitted.

### Proposed Target

Keep typed protobuf requests. Introduce one explicit typed daemon command
model:

```text
DaemonCommand
  decode(envelope)
  authorization_scope()
  idempotency_operation()
  execute(context)
  response_kind()
```

The model may be an enum with methods or a small explicit command registry.
Avoid a macro or generated framework that hides authorization and mutation
behavior.

Separate command families remain:

- daemon administration;
- user session commands; and
- root session administration.

Runtime guard and hook traffic do not enter this command model.

### Behavior That Must Remain

- Header validation occurs before domain dispatch.
- Unix peer identity remains authoritative.
- Root-only commands remain wire-distinct and audited.
- Exact received protobuf bytes remain part of the idempotency fingerprint.
- Read-only requests reject the daemon idempotency header.
- Mutating requests require it.

### Risks

- A generic command framework can obscure authorization.
- Combining daemon-control and runtime-guard messages would violate the
  service boundary.
- Response streaming for logs/events should remain explicit rather than forced
  into a unary abstraction.

### Acceptance Evidence

- One inventory test covers every accepted message kind and its mutability,
  authorization scope, operation identity, and response family.
- Cross-service and header-rejection tests remain.
- Root-only command audit tests remain.

### Decision

- [ ] Approve after S1-S7
- [ ] Reject
- [ ] Revise

Review notes:

## Architectural Simplification Tier

The original S1-S8 suggestions remain above. They are useful, but most improve
ownership, failure handling, extensibility, or resource bounds without making
the product architecture materially smaller.

The `SS` tier applies a stricter question: **what can Erebor stop having?**

| ID | Architectural concept removed | Recommendation | Decision |
| --- | --- | --- | --- |
| SS1 | One runtime-guard listener/server/thread pool per session | One daemon-wide listener using projected-socket access and token plus peer authentication | Implemented and verified |
| SS3 | Closed runner switches plus one universal cross-runner helper protocol | Approve before adding another runner | Implemented and verified |
| SS4 | Public create/start lifecycle | Reject removal: it is product direction, not simplification | Rejected |

`SS2` is intentionally retired rather than renumbered. The analysis is
preserved below as existing plan obligation E1 because the master plan already
assigns generic foreground removal to Phase 3 and final Codex foreground
removal to Phase 4. It requires no separate Phase 2 decision.

## SS1: One Runtime-Guard Server, Many Session Registrations

### Recommendation

Approved and implemented. Token-plus-peer authentication is the session
boundary on the shared guard socket, while private runner projection controls
where governed workloads can reach that socket.

### Architecture Before SS1

`RuntimeGuardService::start_session_with_token` in
`crates/erebor-runtime-session/src/runtime_interception_broker/service.rs`
creates a new `RuntimeInterceptionBrokerServer` for every active session.

Every server creates:

- one Unix socket and session directory;
- one acceptor thread;
- four worker threads by default;
- one queue and connection budget; and
- one independent shutdown lifecycle.

This is surprising because `RuntimeInterceptionBrokerServer` in
`runtime_interception_broker/server.rs` is already designed as a multi-session
server. It owns a `HashMap<String, SessionRegistration>`, and its protocol in
`runtime_interception_broker/protocol.rs` binds a connection only after
checking:

- the declared session ID;
- a unique 128-bit session token;
- the observed Unix peer UID; and
- where required, the observed peer PID.

The code therefore has a multi-session authenticated router inside a
per-session server wrapper.

### What The Per-Session Server Actually Buys Us

There are real benefits:

- the Unix socket path and directory add a kernel filesystem-access boundary;
- removing one socket immediately prevents new connections for that session;
- each session has independent worker/queue capacity, so another session
  cannot consume it; and
- shutdown is mechanically simple because dropping the registration also
  shuts down that session's complete server.

Those benefits are not free. The current protocol still requires the token,
session ID, and peer evidence even on the private socket. Sessions belonging
to the same Unix user also share the same user-level directory permissions, so
the token remains the important session-specific credential. Independent
capacity creates approximately five guard threads per session and prevents the
daemon from having one honest service-wide capacity limit.

### Verdict

The per-session listener is justified only if Erebor intends the socket
pathname itself to be a mandatory, independent security boundary or intends to
reserve dedicated execution capacity for every session.

That is not the current protocol design. The current design already treats
the token, session registration, peer UID, and optional PID as authoritative.
The extra server is defense in depth, while its process-wide cost and lifecycle
duplication are architectural.

### Implemented Target

Run exactly one runtime-guard service inside `erebord`:

```text
erebord
  daemon control listener
  runtime guard listener
    one bounded accept/worker system
    session registration table
      session A -> token, peer constraints, router
      session B -> token, peer constraints, router
```

`RuntimeGuardService::register_session` should only add a logical registration
and return an endpoint containing:

- the shared guard socket path;
- that session's unique token; and
- the existing protocol/deadline information.

The same shared socket can be projected into each Linux mount namespace or
Docker container at a session-local path. The workload does not need to know
that the underlying host socket is shared.

`unregister_session` removes the route and invalidates the token. The service
listener and workers live until `erebord` shuts down. Per-session connection
and queue counters remain logical entries inside the one service rather than
separate servers.

### Socket Access Decision

Erebor uses one shared host socket below a root-traversable-only runtime
directory and projects that socket only into a runner that advertises physical
interception. Each workload sees the session-local path
`/run/erebor/runtime-interception.sock` and receives only its own token. The
service authenticates the declared session against the token and observed Unix
peer UID, with observed PID matching when required. Global and per-UID limits
protect the shared host listener; a per-session limit protects each logical
registration.

### What This Removes

- one listener lifecycle per session;
- one socket directory per session solely for guard transport;
- one acceptor and worker pool per session;
- `SessionInterceptionRegistration::shutdown_server`; and
- separate server construction/teardown from session start and cleanup.

The daemon control and runtime guard remain two separate services. SS1 merges
sessions within the guard service; it does not merge guard traffic with the
CLI control service.

### Acceptance Evidence

- 1, 10, and 100 registered sessions use the same listener and fixed worker
  count;
- crossing session A's token with session B's declared ID fails before routing;
- wrong UID and required-PID mismatches fail before routing;
- unregistering A immediately denies new A traffic without affecting B;
- global, per-UID, and per-session overload fail closed; and
- every runner that advertises physical interception receives only its private
  projected endpoint and its own token.

### Decision

- [X] Approve one daemon-wide listener

Review notes:

### Implementation Result

State: **Done — 2026-07-20.**

- `runtime_interception_broker/service.rs` constructs one server at service
  startup and stores only logical `(observed UID, session ID)` registrations.
  Registering or stopping a session no longer creates or destroys a listener.
- `runtime_interception_broker/server.rs` owns the UID/session registration
  key, token and peer constraints, and per-session connection permits.
- `runtime_interception_broker/platform.rs` owns one acceptor, one bounded
  queue, one fixed synchronous worker pool, global/per-UID connection permits,
  connection deadlines, and service-wide shutdown.
- `runtime_interception_broker/protocol.rs` binds a connection to the exact
  observed UID/session registration after hello and routes later messages only
  through that bound registration. Wrong UID, crossed token/session identity,
  and revoked registration fail before domain dispatch. It clones the narrow
  session router before dispatch so a slow handler never holds the global
  registration-table lock.
- `erebor-runtime-daemon/src/session_api/admission.rs` records the shared host
  endpoint while retaining the private workload endpoint. Daemon control
  remains a different listener, protocol family, dispatcher, and capacity
  owner.
- The installed systemd-container probe exercises two host UIDs, Linux private
  bind projection, Docker runner coexistence, registration revocation, and the
  single root-owned shared socket.

Acceptance evidence on the final source state:

- runtime-guard unit coverage: 35 passed, including 100 simultaneous logical
  registrations on one listener with a fixed worker count, exact UID/session
  routing, crossed credentials, global/per-UID/per-session limits, revocation,
  shutdown, and proof that a slow session handler does not block another
  session's registration or hello;
- browser CDP lifecycle regression: 1 passed;
- privileged installed-product acceptance
  `shared_runtime_guard_service_runs_in_systemd_container`: 1 passed, 2
  filtered out;
- `bash .github/scripts/verify-rust-ci.sh`: passed formatting, workspace check,
  warnings-denied all-target/all-feature clippy, and the full workspace
  all-target/all-feature test run.

The verification run also exposed a pre-existing parallel test-path collision
in `erebor-runtime-filesystem` where wall-clock nanoseconds were reused. Its
test owner now allocates process-local atomic nonces; the filesystem suite
passed three consecutive 57-test runs before the final repository gate.

The service, server, platform, and protocol files remain above the usual
300-line guideline because each is a cohesive owner and each contains its
owner-local tests: logical service lifecycle, authenticated registration,
Unix listener/capacity lifecycle, and bound protocol dispatch respectively.
Splitting those paths would fragment the state machine; unrelated behavior was
not added to them.

## Existing Plan Obligation E1: One Session Engine And One Durable Session Store

Former identifier: `SS2`.

### Recommendation

Do not approve or implement this as a separate Phase 2 simplification. Preserve
the already-planned clean cutover:

- [Phase 3](phase-3-local-stores-generic-adapter-and-cli-migration.md) migrates
  every generic and non-Codex command to `erebord`, removes the generic direct
  foreground adapter, removes adoption from the new product, and makes the
  daemon store authoritative without importing the old `SessionRegistry`;
- [Phase 4](phase-4-codex-adapter-final-cli-cutover-and-app-server-migration.md)
  migrates the one temporary direct Codex exception and then deletes the final
  `SessionExecutionService` launch wiring and old runtime-JSON command path.

This split deliberately keeps the product usable at each phase boundary. Phase
3 leaves only the named Codex exception; Phase 4 removes it after the real
daemon-owned Codex acceptance passes.

### Current Architecture

Erebor currently has two different session products in the same binary:

1. The legacy foreground path in
   `crates/erebor-runtime-cli/src/cli/session.rs` reads `RuntimeConfig`, builds
   `SessionRunPlan`, and invokes `SessionExecutionService` directly in the CLI.
   It uses the older `SessionRegistry` and owns commands such as foreground
   `session run`, diagnostics, adoption, and local review.
2. The daemon path sends typed requests to `erebord`, which uses
   `SessionManager`, `SessionRepository`, immutable `SessionSpec`, daemon
   idempotency, privileged staging, and active runners.

These are not two implementations of one interface. They have different
authorities, plans, state stores, lifecycle behavior, output paths, and
security properties.

### Why This Is Architectural Duplication

Every session feature must either be implemented twice or remain ambiguous:

- Which session ID namespace does `ls` read?
- Which runner/config model is authoritative?
- Does a command survive CLI disconnect or daemon restart?
- Which output store and review source is complete?
- Can adoption satisfy the private-filesystem and startup-control invariants?

The master daemon plan already says the CLI must always call the daemon and
that adoption cannot provide equivalent guarantees. Keeping the foreground
engine during the migration is practical, but treating it as a long-term
architecture is not.

### Simpler Target

There is one session system:

```text
erebor CLI
  -> daemon control API
    -> SessionManager
      -> SessionRepository
      -> selected RunnerDriver
```

- `erebor run` always creates a daemon-owned session.
- logs, wait, stop, kill, inspect, list, review, retention, and remove always
  read or mutate the daemon repository.
- diagnostics are normal daemon-owned runs using an admitted diagnostic
  package/entrypoint; they are not a second execution engine.
- unsupported adoption is removed instead of emulated.
- the client may attach interactively, but disconnecting it does not transfer
  lifecycle ownership out of the daemon.

After migration, delete the foreground production path:

- `SessionExecutionService`;
- `SessionRunPlan` and foreground runner command plans that exist only for it;
- `SessionAdoptionService` and the `session adopt` CLI;
- the older `SessionRegistry` as a second session database; and
- CLI-side session execution and local registry review.

Shared policy, audit, surface, and runner utilities may remain, but they are
called by daemon-owned sessions rather than by a second owner.

### What Must Exist Before Deletion

- one easy `erebor run` flow;
- foreground attach or log-follow behavior for users who want terminal output;
- daemon-backed `ls`, `inspect`, `logs`, `wait`, and review;
- a daemon-owned diagnostic run replacement; and
- migration of the Codex example to the daemon path.

These are replacement capabilities, not compatibility shims.

### What This Removes

- one execution engine;
- one session database and ID namespace;
- one configuration-to-run-plan model;
- one set of CLI lifecycle semantics; and
- the false product promise that adopted and daemon-created sessions are
  equivalent.

### Acceptance Evidence

- no production CLI path invokes `SessionExecutionService`;
- every public session command connects to `erebord`;
- one session ID is visible consistently to run, logs, review, and lifecycle
  commands;
- CLI disconnect leaves the daemon-owned session unchanged; and
- removal of the old engine leaves no production consumer of its registry or
  run-plan types.

### Existing Phase Ownership

No independent decision is required here. Phase 3 and Phase 4 approval,
implementation, and acceptance evidence govern this removal. If either phase
changes, update this cross-reference rather than creating a competing Phase 2
implementation slice.

## SS3: One Runner Extension Boundary, No Universal Helper Layer

### Recommendation

Approve before adding macOS, Kubernetes, or another runner.

### Original Daemon Architecture (Removed)

Before SS3, daemon runner selection was repeated across the system:

- `SessionRunnerKind` is a closed Linux/Docker enum in core configuration;
- generic `SessionSpec` validation matches Linux versus Docker;
- Linux and Docker runner implementations live in
  `erebor-runtime-core/src/session/`;
- both runners launch the same `SessionHelperLaunchConfig` protocol; and
- `erebor-runtime-session/src/session_helper/workload.rs` matches the runner
  kind again to create `LinuxWorkload` or `DockerWorkload`.

The actual call path is therefore:

```text
SessionManager
  -> generic SessionRunner
    -> generic helper protocol/process
      -> runner-kind switch
        -> LinuxWorkload or DockerWorkload
```

Adding a runner is not one extension. It requires edits to central enums,
generic validation, runner construction, helper handoff, helper workload
dispatch, recovery, and usually CLI/config matches.

The separately live foreground configuration and execution path still uses
`SessionRunnerKind`. It is not stale code: the legacy `erebor run` path and
agent configurations call it today. Its daemon cutover and removal belong to
master-plan Phases 3 and 4; SS3 must not delete that live behavior while those
replacement commands and adapters do not yet exist.

### Simpler Target

Use one registry boundary keyed by a validated `RunnerId`:

```text
RunnerRegistry
  "linux-host" -> LinuxRunnerDriver
  "docker"     -> DockerRunnerDriver
  "kubernetes" -> KubernetesRunnerDriver
```

One `RunnerDriver` owns:

- capability inspection;
- admission of its runner-specific request;
- preparation of its execution input;
- start and stable recovery identity;
- stop, kill, health, and remove; and
- whether it needs an auxiliary controller process.

The durable generic record keeps the runner ID, implementation identity, and a
versioned runner-owned recovery value. Generic code does not match runner IDs
or interpret runner-specific fields.

Remove the assumption that every runner uses the same session helper:

- Linux may keep a private controller process because it needs a parent that
  captures output, owns process-group control, and implements daemon-loss
  behavior.
- Docker can use the container runtime as its durable supervisor or use a
  Docker-specific controller if its proven continuity design needs one.
- Kubernetes would use Kubernetes objects and watches, not a Linux helper
  process pretending to be generic.

Common output pumps, framing, and process utilities remain library code. They
do not require a universal `SessionHelperCommand`, `SessionHelperEvent`,
`SessionHelperHandoff`, or `Workload::{Linux,Docker}` protocol shared by every
runner.

### Relationship To S3 And S4

S3 and S4 remain useful boundary and continuity reviews. SS3 is the simpler
architectural destination beneath them: runner ownership is not merely moved
out of core; the duplicate runner switch and mandatory universal helper layer
are removed.

### What This Removes From The Daemon Path

- the closed runner enum as the extension mechanism;
- runner-specific matching from generic spec validation;
- the second runner dispatch inside the helper;
- the rule that every runner has a helper process; and
- edits to central modules merely to register a new runner.

### Acceptance Evidence

- adding a test runner requires a new driver and one registry entry, with no
  new match arm in generic session code;
- Linux and Docker durable records round-trip opaque versioned recovery data;
- generic `SessionSpec` validation contains no Linux/Docker/Kubernetes branch;
- Docker has no helper process unless its own continuity proof requires one;
  and
- Linux daemon-loss behavior remains explicit and tested by its driver.

### Decision

- [x] Approve
- [ ] Reject
- [ ] Revise

Review notes:

Implemented on 2026-07-20.

- `RunnerId` is a validated open identifier. `RunnerRegistry` maps it to one
  `RunnerDriver`; the daemon/session manager contain no Linux/Docker selection
  match.
- `SessionSpec` generic validation validates only common structure. The
  original SS3 implementation still left a fixed `RunnerAdmissionProfile` in
  daemon admission; its three Linux/Docker-oriented switches meant a new driver
  could not own its complete execution admission. The 2026-07-21 completion
  replaces that profile with a `RunnerAdmissionRequest` and
  `RunnerExecutionAdmission`: the registry safely resolves common workspace
  identity, then the selected driver admits its executable or image, privilege
  plan, filesystem projections, and endpoint projections. Daemon admission
  only parses generic request/fixture data and persists the driver result.
- Runner preparation is now chosen by the driver too. `SessionManager` passes
  a narrow `RunnerPreparation` resource facade to the selected driver; it no
  longer interprets the capability's physical-interception flag. The Linux
  driver stages execution and explicitly starts its session-local runtime
  guard. The Docker driver stages execution without creating a guard. The
  daemon runtime retains the privileged mount, output-store, credential, and
  guard-service operations without becoming a runner switch.
- `RunnerInstallConfig` is now a generic named-program override map. Linux
  and Docker each own their default paths and the names of the programs they
  require, so adding another driver does not add a field to a central
  Linux/Docker-shaped installation configuration object.
- `RunnerBinding` stores the runner ID, implementation ID, observation time,
  and an opaque `RunnerRecovery { format_version, payload }`. Generic code
  persists and passes the value without parsing it. Linux and Docker driver
  tests round-trip their own JSON recovery schemas.
- The common `SessionHelperHandoff`, command/event types, and `HelperWorkload`
  enum were removed. Linux and Docker have separate controller protocols and
  binaries under `erebor-runtime-session/src/runners/`.
- Docker retains a Docker-only controller for the current continuity proof:
  the Phase 2 capability supports daemon-loss `terminate` and requires durable
  stdout/stderr capture. A detached Docker container cannot observe the daemon
  control-pipe EOF or keep Erebor's required output pumps alive by itself. This
  is an explicit driver-owned exception allowed by SS3, not a mandatory helper
  abstraction for future runners.
- The still-live foreground `SessionRunnerKind` implementation is deliberately
  retained. It is not part of the daemon runner registry and is not an SS3
  extension point; Phases 3 and 4 own its replacement and eventual removal.

Implementation state: **Done**. The runner-owned admission and preparation
completion was verified on 2026-07-21. The exact commands and outcomes are
recorded in the result below.

## SS4: One Public `Run` Operation, Internal Create/Start States

### Recommendation

Approve.

### Current Architecture

The daemon protocol exposes session creation and session start as separate
mutations with separate request/response messages, idempotency operation names,
and client commands.

Internally, the split is necessary: Erebor must durably persist `created`
before acquiring resources, then move through `starting` before `running`.
Externally, however, a session is one invocation and cannot be restarted after
it becomes terminal. The user normally wants to run it, not create a dormant
session object and start it later.

### Simpler Target

Expose one idempotent `RunSession` operation:

```text
RunSession request
  -> authenticate and admit
  -> persist created
  -> prepare and start
  -> return session identity and current state
```

`created` and `starting` remain durable internal recovery states. They stop
being public lifecycle commands.

One idempotency record owns the complete operation. Replaying after a crash
loads the stored admitted spec and current durable state, then either resumes
the pending start or returns the already-started result. A failed run remains
inspectable as a terminal session with its evidence.

If callers need validation without execution, provide a read-only `run
--check`/admission-check operation. Do not preserve public create merely as a
validation surrogate.

The API may later offer an advanced internal scheduling primitive, but it
should not shape the default local product unless a real scheduler requires
dormant admitted sessions.

### Docker Inspiration

Docker retains separate `create` and `start` because a container is a reusable
object that may be started again. Erebor explicitly defines a session as one
non-restartable governed invocation. Docker's convenient `run` shape fits
Erebor better than copying Docker's reusable-container lifecycle.

### Existing Plan Conflict

This is a real new product-contract decision, not work already assigned to a
later phase. The current master plan and Phase 3 explicitly expose
`erebor create | run | start`, define `run` as create followed by start, and
require generic create-without-start acceptance evidence. Phase 6 also carries
that lifecycle into its runner-parity matrix.

Approving SS4 therefore requires amending the master plan, Phase 3, Phase 6,
and `lifecycle-probe.md` before implementation. It must not be implemented as
an extra Phase 2 change while the later phases still require the opposite
public contract.

### What This Removes

- public `session create` and `session start` commands;
- one public lifecycle edge and its user-facing error cases;
- separate create/start idempotency records;
- create/start response mapping in client and daemon; and
- the possibility of indefinitely dormant user-created sessions.

### Acceptance Evidence

- one idempotency key cannot create or launch more than one workload;
- crash replay at admitted, created, starting, and running returns one session
  identity and never duplicates resources;
- the durable repository still proves `created` precedes resource creation;
- a start failure is returned as a failed inspectable session; and
- CLI help presents `erebor run`, not a required create/start sequence.

### Decision

- [ ] Approve
- [ ] Reject
- [ ] Revise

Review notes:

## SS Scheduling Against Existing Phases

- Decide **SS4** before implementing Phase 3 because it changes Phase 3's
  public create/run/start contract.
- **E1** is not an SS implementation slice: Phase 3 performs the generic
  cutover and Phase 4 performs the final Codex cutover.
- **SS1** was implemented as an internal runtime-guard simplification after
  its socket-access decision, without merging it into E1.
- **SS3** was implemented before adding another runner or reaching Phase 6's
  runner-extension work.

Each remaining active SS item needs its own implementation plan after
approval. This record authorizes and records only completed S1, SS1, and SS3.

## Original S-Tier Proposed Target Ownership

This is the ownership target proposed by the original S1-S7 suggestions. It is
preserved for their review and does not override the SS architectural choices
above. If S1-S7 are approved, the intended high-level ownership is:

```text
erebor-runtime-core
  session/
    spec/                 immutable generic session contract
    lifecycle.rs          allowed durable state transitions
    runner.rs             RunnerId, capability, runner/active contracts

erebor-runtime-session
  session_manager/        only lifecycle transaction owner
  session_repository/     crash-safe durable record store
  session_layout.rs       trusted path derivation
  session_output/         durable streams and evidence sinks
  runners/
    linux/                Linux admission/start/recovery/continuity
    docker/               Docker admission/start/recovery/continuity
  runtime_interception_broker/
    service.rs            separate runtime-guard service
    executor.rs           shared bounded guard capacity
    platform.rs           socket transport
    server.rs             guard protocol dispatch

erebor-runtime-daemon
  control/                daemon control transport/authentication
  session_api/            protobuf/domain conversion and root constraints
  idempotency/            durable command replay
  path_broker/            privileged caller-identity path resolver process
```

This shape does not require every proposed item to become a separate file. It
defines ownership, not a line-count exercise.

## Original S-Tier Provisional Implementation Slices

This original order applies only if the corresponding S-tier suggestions are
approved. It does not authorize or sequence the SS tier. Implement one slice
at a time and stop after its checkpoint.

### Slice 0: Decision And Baseline Lock

Purpose:

- resolve every pending decision required by the first implementation slice;
- record the exact pre-refactor test state;
- preserve the known Linux `continue` recovery failure as a failing acceptance
  that the continuity slice must fix; and
- inventory every current Phase 2 protocol message, durable field, active
  resource, and cleanup path.

Checkpoint:

- no code change;
- approved decisions are written into this document;
- baseline commands and exact results are recorded.

### Slice 1: Remove Approved Unreachable Contracts

Applies S7 only.

Purpose:

- reduce the model and protocol before moving their owners;
- avoid carrying unreachable attach/lease/cursor fields through the refactor.

Checkpoint:

- protocol and model tests pass;
- all remaining fields have named production owners;
- hands-on examples use only the remaining protocol.

### Slice 2: Generic Session And Runner Contracts

Applies S3 and S6.

Purpose:

- compose `SessionSpec`;
- remove the field-for-field admission mirror;
- define the approved runner identifier;
- define the versioned opaque recovery handle; and
- leave Linux/Docker behavior unchanged while preparing the ownership move.

Checkpoint:

- core contains generic contracts only;
- serialized schema tests pass;
- no generic spec validation matches Linux versus Docker.

### Slice 3: Runner-Owned Implementations And Continuity

SS3's extension-boundary simplification is implemented. The remaining S3 and
S4 ownership/hardening reviews are not approved by that decision.

Purpose:

- keep Linux/Docker daemon-owned drivers in the session crate;
- retain each driver's proved continuity mechanism;
- keep the common runner/helper switch removed; and
- review any remaining S3/S4 recovery hardening separately.

Checkpoint:

- Linux and Docker own start/recovery/cleanup end to end;
- the Linux `continue` acceptance passes;
- `terminate` and `continue` pass for each advertised runner;
- corrupted/stale recovery handles fail honestly.

### Slice 4A: Session Manager

Applies S1. Implemented on 2026-07-20.

Purpose:

- establish one lifecycle transaction owner;
- move guard/output/staging cleanup behind that owner; and
- reduce daemon session code to protocol/admission mapping.

Checkpoint:

- fault injection proves rollback after every preparation/start step;
- one owner commits terminal states and cleanup;
- no duplicate resource cleanup match remains in daemon protocol code.

### Slice 4B: Prepared Resources

Applies S2 only and remains unapproved.

Purpose:

- introduce `SessionLayout` and `PreparedSession`;
- replace manual preparation compensation with explicit resource transfer and
  rollback; and
- narrow runner input beyond `OutputEndpoints`.

Checkpoint:

- fault injection proves rollback after every acquired resource;
- repeated rollback and terminal cleanup are harmless; and
- path derivation has one deterministic owner.

### Slice 5: Retired Into SS1

S5 has no independent implementation slice. SS1 owns the shared listener,
fixed worker pool, bounded queue, and logical global/per-UID/per-session
limits. Do not add a second executor or restore per-session host listeners.

Purpose:

- preserve private workload-facing endpoint projection and credentials;
- use the SS1 guard-service capacity owner; and
- retain the global, per-UID, and per-session fairness/overload evidence.

Checkpoint:

- worker count remains bounded as session count grows;
- cross-session isolation and service separation remain;
- per-session and service-wide drain tests pass.

### Slice 6: Optional Command Dispatch Consolidation

Applies S8 only if separately approved after the final command inventory.

Checkpoint:

- one explicit command metadata inventory exists;
- authentication, authorization, idempotency, audit, and response semantics
  remain visible and tested.

### Slice 7: Final Verification And Plan Reconciliation

Purpose:

- run complete local and privileged acceptance;
- update Phase 2 and the master plan from actual final behavior;
- remove stale paths/symbols from later phase documents; and
- mark this proposal and Phase 2 accurately.

Phase 3 remains blocked until the user approves it separately.

## Required Verification

Each implementation slice must add or update code-backed tests beside its
owner. The final source state must run:

```sh
bash .github/scripts/verify-rust-ci.sh
```

Focused iteration should include:

```sh
cargo test -p erebor-runtime-core
cargo test -p erebor-runtime-ipc
cargo test -p erebor-runtime-client
cargo test -p erebor-runtime-daemon
cargo test -p erebor-runtime-session
cargo test -p erebor-runtime-e2e --test daemon_control_plane
```

The installed privileged acceptance must include:

```sh
cargo test -p erebor-runtime-e2e --test daemon_control_plane \
  required_session_output_and_retention_contract_runs_in_systemd_container \
  -- --ignored --nocapture

cargo test -p erebor-runtime-e2e --test daemon_control_plane \
  daemon_control_plane_runs_in_systemd_container \
  -- --ignored --nocapture
```

The lifecycle matrix must prove:

- create without start;
- normal start and natural exit;
- stop, kill, wait, remove, prune, and retention hold;
- required-output disk failure;
- daemon SIGKILL and restart for every advertised runner/failure mode;
- guard overload, fairness, per-session drain, and service drain;
- stale/corrupt recovery handles;
- preparation failure after every owned resource; and
- two users and multiple sessions remaining isolated.

The cumulative hands-on example remains
`examples/codex-app-server`, but the runtime behavior under test is generic and
must not depend on Codex-specific semantics.

## Verification Findings Outside The Approved Simplifications

The earlier filesystem-suite failures were a parallel test-fixture path
collision. The test owner now uses a process-local atomic nonce. The earlier
Linux `continue` recovery failures are also resolved. SS3's Linux driver owns a
controller recovery schema that verifies both controller and workload identity
after daemon restart. The Linux process guard now also paces failed requests at
the common broker boundary while keeping the tracee stopped, so a temporary
daemon/broker outage remains fail-closed without producing a process and
required-stderr storm. The final repository and privileged installed-product
evidence is recorded below and in the parent Phase 2 result.

## Stop Point

S1, SS1, and SS3 are implemented. S5 is retired into SS1. Stop before S2-S4,
S6-S8, and SS4 until the user explicitly approves an exact suggestion and asks
to begin it. E1 remains owned by future Phases 3 and 4.

## Simplification Result

State: **Done for approved items.** S1, SS1, and SS3 are implemented and
verified. SS3 now includes its runner-owned admission boundary: no fixed
Linux/Docker profile remains in daemon admission, and its selected driver owns
execution-resource preparation. S5 is retired because SS1 absorbed its valid
capacity requirements and removed its per-session-listener premise. S2-S4,
S6-S8, and SS4 remain unapproved; former SS2 is only a cross-reference to the
existing Phase 3/4 cutover obligation.

S1 established `SessionManager` as the only session lifecycle transaction
owner and reduced the daemon owner to `DaemonSessionApi`. SS1 established one
daemon-wide runtime-guard server with logical UID/session registrations. SS3
established the open `RunnerId`/`RunnerDriver` registry, opaque versioned
runner recovery, runner-owned execution admission, generic spec validation
without runner-name branches, and separate Linux/Docker controller protocols.
The daemon now passes generic trusted inputs to the registry; each selected
driver returns its executable or image identity, privilege plan, and projected
execution resources, then explicitly chooses the daemon-owned preparation it
needs. The generic runtime no longer uses a runner capability flag to decide
whether to start a guard. Runner installation defaults and program names also
live with the relevant driver, rather than in a Linux/Docker-shaped central
configuration object. The still-live foreground `SessionRunnerKind` path is
not stale; its replacement/removal remains owned by Phases 3 and 4. This result
does not authorize or claim the remaining S2-S4, S6-S8, or SS4 behavior.

Verification on the final approved-simplification source state:

- 2026-07-21 SS3 runner-owned admission and preparation completion:
  `cargo check --workspace`, `cargo test -p erebor-runtime-session --lib`
  (123 passed),
  `cargo test -p erebor-runtime-daemon --lib` (13 passed, 5 intentionally
  ignored), and warnings-denied workspace all-target/all-feature clippy all
  passed. The full `bash .github/scripts/verify-rust-ci.sh` passed after the
  final Rust source edit;

- the freshly built, separate `erebor-daemon-systemd:ss3-verify` image passed
  all three ignored privileged installed-product probes: the full Linux/Docker
  lifecycle and daemon-loss matrix, required output plus retention, and the
  shared runtime-guard service (each 1 passed, 2 filtered out). The existing
  local image was left untouched;

- `cargo test -p erebor-runtime-session --test linux_process_guard_broker`
  passed: 2 passed, including the broker-outage pacing regression;
- `cargo test -p erebor-runtime-session --lib session_manager::tests` passed:
  11 passed;
- `cargo test -p erebor-runtime-daemon` passed: 16 passed and 5 privileged
  host-socket tests ignored by that ordinary invocation;
- `cargo clippy -p erebor-runtime-session -p erebor-runtime-daemon
  --all-targets --all-features -- -D warnings` passed;
- the privileged Docker/systemd acceptance
  `required_session_output_and_retention_contract_runs_in_systemd_container`
  passed when explicitly run with `--ignored --nocapture`;
- the shared runtime-guard privileged Docker/systemd acceptance
  `shared_runtime_guard_service_runs_in_systemd_container` passed;
- the broader privileged Docker/systemd acceptance
  `daemon_control_plane_runs_in_systemd_container` passed in three consecutive
  fresh containers. It covered Linux-host and Docker lifecycle, daemon-loss
  `terminate`/`continue`, restart recovery, runner-controller identity, cgroup
  placement, and cleanup;
- a stronger installed-product stress run with systemd's daemon restart delay
  increased from two to five seconds also passed. Before the guard pacing fix,
  that same isolated change deterministically filled required stderr and
  interrupted the Linux `continue` session; and
- `bash .github/scripts/verify-rust-ci.sh` passed formatting, workspace check,
  warnings-denied all-target/all-feature clippy, and all workspace
  all-target/all-feature tests after the filesystem test-path collision was
  corrected.

S1, SS1, and SS3 therefore have code-backed, installed privileged, and clean
repository-gate evidence. This does not approve or implement the remaining
S2-S4, S6-S8, or SS4 suggestions.
