node:
  runtimeHook:
    socketPath: /run/mithril/test-runtime-admission.sock
  nodeSelector:
    mithril.example/pool: protected
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
              - key: kubernetes.io/arch
                operator: In
                values: [amd64]
control:
  admission:
    caBundle: dGVzdA==
